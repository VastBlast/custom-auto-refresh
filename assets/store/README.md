# Store Assets

Generated listing assets are kept outside the extension package:

- `assets/store/icons/` contains Chrome Web Store and Edge Add-ons icons.
- `assets/store/screenshots/` contains store screenshots.
- `assets/store/promotional/` contains promotional tiles.

Regenerate them with:

```sh
npm run icons
npm run store:screenshots
```
